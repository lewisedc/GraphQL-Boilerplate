endpoint: https://eu1.prisma.sh/lewis-df7b16/test/dev

datamodel: datamodel.prisma

generate:
  - generator: javascript-client
    output: ./generated/prisma-client

seed:
  import: seed.graphql

hooks:
  post-deploy:
    - prisma generate

secret: secret123
